{
  "name": "baseline",
  "version": "1.0.0",
  "description": "A new project to build something amazing",
  "scripts": {
    "dev": "cd planbase && npx tsx --env-file ../.env index.ts",
    "start": "cd planbase && npx tsx --env-file ../.env index.ts",
    "planbase": "cd planbase && npx tsx --env-file ../.env index.ts",
    "install:planbase": "cd planbase && npm install",
    "build": "cd planbase && npm install && npm run build",
    "vercel-build": "echo 'Build completed'"
  },
  "devDependencies": {
    "tsx": "*"
  },
  "engines": {
    "node": ">=20"
  }
} 